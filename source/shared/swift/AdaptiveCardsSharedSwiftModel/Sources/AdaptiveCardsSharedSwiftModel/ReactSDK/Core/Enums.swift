/*
import Foundation

class ContainerStyle {
    static let `default` = "default"
    static let emphasis = "emphasis"
    static let accent = "accent"
    static let good = "good"
    static let attention = "attention"
    static let warning = "warning"
}

enum ActionStyle: String {
    case `default` = "default"
    case positive = "positive"
    case destructive = "destructive"
}

enum ActionMode: String {
    case primary = "primary"
    case secondary = "secondary"
}

enum Size: String {
    case auto
    case stretch
    case small
    case medium
    case large
}

enum ImageSize: String {
    case small
    case medium
    case large
}

enum HorizontalAlignment: String {
    case left
    case center
    case right
}

enum VerticalAlignment: String {
    case top
    case center
    case bottom
}

enum ActionAlignment: String {
    case left
    case center
    case right
    case stretch
}

enum ImageStyle: String {
    case `default`
    case person
}

enum ShowCardActionMode: String {
    case `inline`
    case popup
}

enum Orientation: String {
    case horizontal
    case vertical
}

enum FillMode: String {
    case cover
    case repeatHorizontally
    case repeatVertically
    case `repeat`
}

enum ActionIconPlacement: String {
    case leftOfTitle
    case aboveTitle
}

enum InputTextStyle: String {
    case text
    case tel
    case url
    case email
    case password
}

enum ValidationPhase: String {
    case parse
    case toJSON
    case validation
}

enum ValidationEvent: String {
    case hint
    case actionTypeNotAllowed
    case collectionCantBeEmpty
    case deprecated
    case elementTypeNotAllowed
    case interactivityNotAllowed
    case invalidPropertyValue
    case missingCardType
    case propertyCantBeNull
    case tooManyActions
    case unknownActionType
    case unknownElementType
    case unsupportedCardVersion
    case duplicateId
    case unsupportedProperty
    case requiredInputsShouldHaveLabel
    case requiredInputsShouldHaveErrorMessage
    case other
}

enum TypeErrorType: String {
    case unknownType
    case forbiddenType
}

enum ThemeName: String {
    case light
    case dark
}
*/
